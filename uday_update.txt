---- 17-09-2026 --------
Daily Update :  PySpark Advanced Data Operations & Mentorship:
Data Aggregations & Joins: Explored PySpark grouping mechanisms (groupBy()) and combined datasets across relational keys using join().

DataFrame Merging: Practiced combining rows across DataFrames using union(), unionAll(), and schema-aware matching with unionByName().

String Manipulations: Reviewed core Python string data concepts along with built-in functions for text processing.

Peer Mentorship: Led a review session explaining string handling and predefined functions to the rest of the batch.
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
---- 16-09-2026 --------
Daily Update :  PySpark Fundamentals & DataFrame Operations:
DataFrame Manipulations: Practiced selecting specific columns (select()), adding/modifying columns (withColumn()), and renaming existing fields (withColumnRenamed()).

Data Filtering & Aggregations: Filtered datasets using condition logic (where(), filter()) and performed relational grouping operations (groupBy()).

Data Cleaning & Sorting: Dropped redundant columns and duplicate records (drop(), dropDuplicates()) and ordered data outputs (orderBy(), sort()).

Core Concepts & Actions: Explored the underlying PySpark Column Class functionality and fetched action results using collect().
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
---- 12-09-2026 --------
Daily Update :  Web Application & Django Set Up:
Environment & Setup: Cloned the Django project repository from Git and set up the Python virtual environment.

Database & Migrations: Executed Django database migrations to create the required tables in the database.

Data Management: Handled data seeding/importing and exported dataset backups for local development.

Skill Enhancement: Conducted a morning Python review session to prepare for mock interviews.
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
---- 11-09-2026 --------
Daily Update: Web Application & Django Basics:
Learned the core concepts of web applications and how the Django framework is used for back-end development.

Explored the MTV (Model-Template-View) architectural structure that Django uses to separate data, logic, and presentation layers.

Practiced setting up a Django environment and installing the framework using the pip install django command.

Discovered Django's built-in features, such as the automatic administrative portal and object-relational mapping (ORM) for easy database management.

Gained hands-on understanding of how Django handles URL routing to connect user requests with server-side logic.
