---24-09-26----
Today i learned Exception handling concept in python used to handle runtime error without stopping  the entire program unexceptedly.

Exception handling contain maily four blocks is their 1.try – Contains code that may cause an exception, 2.except – Handles the exception
3.else – Executes when no exception occurs, 4.finally – Executes whether an exception occurs or not.

I practiced some of the programs using try , except, else, finally and understand how its work apart form their are some of the importent concepts 
in exception handling like zerodivisionerror, valueerror keyerror ect.

After i practiced some of the importent  questions based on datatypes, strings, operators, conditional statements and i understand each and every logic 
of the program.

---23-09-26----
Today i learned File handling concepts in python to create, read, write, append, & create new file, update and manage files.

syntax open file: open("filename", "mode")

I worte the file handling concept codes into visual studio and understand how its work and applied different modes(r,w,a)
In file handling concept i learned another topic encoding it working with text files, UTF-8 is a common choice i understand encoding each and 
every language some code will run on the backend that is also called encoding.

EX:with open("student.txt", "w", encoding="utf-8") as file:
    file.write("Hello Python")
I learned string functions and inside operations on string like startswith, endswith, upper, lower, index, split, find, center these 
are the concepts i learned today.

      ----22-09-26----
Today i learn  using python and django frame code to built a web application school management system.

Inside this application their are three components is their students details, workers details, teachers details in this project
we used  database sql CRUD operations it menas C- create , R- retrive the data U- update the data, D- delete the data.

Main agenda of the project is to built a web appliction add the students, teachers, workers and manage the large amount of data.


some of the HTML code used in this project to view how much length we want to give while entered the details.

COMMANDS USED IN THIS PROJECT:
1. Open VS Code from CMD: code .
2. Open Terminal in VS Code: Ctrl + `
3. Clone Repo: git clone YOUR_REPOSITORY_URL
4. Create Virtual Environment: python3 -m venv .venv
5. Windows PowerShell Execution Policy Fix: Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
6. Activate Virtual Environment (Windows): .venv\Scripts\Activate.ps1
7. Activate Virtual Environment (Mac/Linux): source .venv/bin/activate
8. Install Requirements: python -m pip install -r requirements.txt
9. Run Migrations: python manage.py migrate
10. Seed Database: python seed_data.py
11. Run Server: python manage.py runserver

After running python manage.py runserver  web appliction url will work afer open the url inside we need to add student ,teacher, worker details
main purpose of this application is to manage school management information.                                                       

-----19-09-26----
Today i learned functions in python it is mainly reusable block of the code that performs a specific task insted of writing the 
the same code many times, we write it once inside a function and call it whenever needed.

Basic function: def greet():
    print("Hello, welcome to Python!")

greet()

Their are so many topics in functions.
Function with a parameter: A parameter is a variable that receives a value when the function is called.
Default Parameter: You can give a parameter a default value.
Keyword Arguments: You can specify the parameter name when calling the function.
positional Arguments: It is an argument passed to a function based on its position(order).
**Kwargs: It allows a function to receive mltiple keyword arguments.

These are the main function concepts i learned and practiced.

I worte the calculator code using functions concept and understand how it's work.

----18-09-26----
Today i revised and practiced Data structure concepts.

I learned another topic Variables, Values and operators python data types(int, float, str, bool, None).

Arithmetic operators like(add, sub, mul, div, floor, mod, pow) and i builted calculator program using python code.

String slicing means taking a part of a string using its positions (indexes) string[start:stop:step].
Example:
Positive slicing                         Negative slicing
------------------                      ---------------------
text = "PYTHON"                          text = "PYTHON"

                                          print(text[-4:-1])                                            
print(text[0])
print(text[2])
print(text[-1]) 

----17-09-26-----
Today i learned python Data structure concepts and inside methods i Practiced.

Data structure concepts List, Tuple, Set, Dictionary.

List: A list is a Python data structure used to store multiple values in a single variable.

List inside methods:append() ----> adds one item.
                    insert()-----> adds at a specific position.
                    remove()-----> remove by value.
                    pop()--------> remove by index.
                    extend()-----> adds multiple items.
                    clear()------> removes everything.
Tuple: A tuple is a colliction of multiple values stored in a single variable.

Tuple inside methods:count()-----> Counts how many times a value apperars.
                     index()-----> Returns the index of the first occurence of a value.

set: A set is a python data sturcture used to store unique values.

Set inside methods:add()--------> Adds one element to a Set.
                   update()-----> Adds multiple elements in a Set.
                   remove()-----> Remove a specific element in a Set.
                   pop()--------> Remove and eturns an arbitrary element from the set.
                   clear()------> Removes all elements.
                   union()------> Combines all unique elements form two sets.
               intersection()---> Returns elements that are common to both sets.
               difference()-----> Returns elements that exist in the first set but not in the second.
                isdisjoint()----> This checks whether two sets have no common elements.
                is subset()-----> checks whether all elements of the one set exist inside another set.
                is superset()---> checks whether a set contains all elements of another set.
Dictionary: A dictionary is a python data structure that stores data as key-value pairs.

Dictionary inside methods: Keys()---->Returns all Keys.
                           values()-->Returns all values.
                           items()--->Return key-value pairs.
                           update()--->Used to all new key-value pairs or update existing values.
                           pop()------>Remove a specific key returns its value.
                           popitems()--->Remove and returns the last inserted key- values pairs.
 and some methods clear(), copy(), len() ect these data structure concept and inside methods i learned and Practiced.                          


-----16-09-26---- 
Today i learned python fundamentals concepts like  basic print statements onwards and variables & Data types.

Python data types str, int, float, bool.

performance of arithmetic operators like(add, sub, mul, div, floor, modulus, power). 

performance of Comparison operators like(equal, not equal, greaterthan, greaterthan or equal, lessthan, lessthan or equal).

performance of Input & type conversion.

performance of conditional statements like(if, if-else, if-eif-else).

performance of loops like(for, while, loop control(break, continue, pass)).

Above these concepts i learned and Practiced.



----------------------------------------------------------------------------------------------------------------------------------------------------------
-----12-09-26----
Today i learned clone the school management data git hub to vs code and inside the admin file, app file, forms, models, tests, urls, views
and open the terminal run these commands python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python manage.py migrate
.venv/bin/python manage.py runserver 

After run the server command one web page should be opened inside details like below.

Today i learned existing school database inside components Student--- details, email.id, phone no, grade level, guardian_name, guardian_phone and 
Teachers details----- name, phone no, subject and qualification Workers----- name,email,phone no, role, department.

If suppose enter some details like studens detail, teacher detail, worker details enter into web page that will display into database.

Go to extensions download SQLITE mainly in this code one of the file is their seed_data.py if suppose inside student detais, worker detail, teacher details are edit inside code 
that will display into school database in SQLite database engine.



----------------------------------------------------------------------------------------------------------------------------------------
-----11-09-26----
Daily Updates: Web Application & Django Development

*Django is a python web framework used to build web applications quickly and securely.

*Django maily follows MVT - Model, View, template

                 Django Application
                        │
          ┌─────────────┼─────────────┐
          ↓             ↓             ↓
       Model          View         Template
          │             │             │
          ↓             ↓             ↓
      Database      Business       HTML/CSS
                     Logic

*In this application performed backend we using view Business Logic(like python functions), Front end Template(HTML/CSS) and databases
 are mainly performed CRUD operations c(Create), R(Read), U(Update), D(Delete).

                 Django Web Application
                          |
                          ↓
                    students/urls.py
                          |
             ┌────────────┴────────────┐
             ↓                         ↓
          views.py                  HTML
             |
             ↓
          models.py
             |
             ↓
          Database


*Mainly API (Application Programming Interface) allows two applications to communicate with each other.



